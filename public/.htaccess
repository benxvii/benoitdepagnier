<IfModule mod_headers.c>
  # Shell SPA (index.html + routes réécrites /marine, /poi, etc.) : toujours revalider.
  # Évite qu'un raccourci iOS garde une vieille version du site.
  <Files "index.html">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </Files>

  # Assets Vite hashés (index-xxxxx.js) : cache long, le nom change à chaque build.
  <LocationMatch "^/assets/.+\.(js|css)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </LocationMatch>
</IfModule>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Forcer HTTPS
  RewriteCond %{HTTPS} off
  RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  RewriteRule ^downloads/PortfolioApp\.zip$ /downloads/Portfolio-win.zip [R=301,L]
  RewriteRule ^index\.html$ - [L]
  # Ne réécrire que les vrais fichiers (images, assets). Les dossiers
  # physiques (ex. galerie photo) doivent aussi retomber sur index.html.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule . /index.html [L]
</IfModule>
